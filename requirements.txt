# 핵심 RPA 자동화 패키지
pyautogui>=0.9.54
pyperclip>=1.8.2

# 이미지 처리 (OpenCV)
opencv-python>=4.8.0
numpy>=1.24.0,<2.0.0
pillow>=10.0.0

# 데이터 처리
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1

# GUI (tkinter는 Python 기본 패키지로 별도 설치 불필요)

# 유틸리티
loguru>=0.7.0

# 테스트 (선택사항)
pytest>=7.4.0
pytest-cov>=4.1.0

# 배포용 (선택사항)
# pyinstaller>=5.13.0
